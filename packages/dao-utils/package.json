{
  "name": "@builderdao/dao-utils",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "type": "module",
  "files": [
    "dist/**"
  ],
  "bin": {
    "dao-sdk-utils": "./dist/cli.cjs"
  },
  "scripts": {
    "dev": "yarn build -- --watch",
    "build": "tsup src/index.ts src/cli.ts --dts --format cjs",
    "lint": "TIMING=1 eslint src --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "keywords": [],
  "author": "@builderdao",
  "license": "ISC",
  "devDependencies": {
    "@builderdao/config": "*",
    "@builderdao/tsconfig": "*",
    "eslint": "^8.9.0"
  },
  "dependencies": {
    "commander": "^9.0.0",
    "plop": "^3.0.5"
  }
}